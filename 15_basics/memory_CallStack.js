// call stack + memory heap

// Memory Heap

const number = 610; // allocate memory for number

var human = {
  name: "Anshu Raj", // allocate memory for human object
};

human = 10;




const sum = (a, b) => {
  return a + b;
};

const calculate = () => {
  const result = sum(2, 3);
  return result;
};
calculate();


